name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          check-latest: true

      - name: Build binaries
        env:
          CGO_ENABLED: "0"
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir -p dist
          targets="linux/amd64 linux/arm64"
          for t in $targets; do
            os="${t%/*}"; arch="${t#*/}"
            echo "building $os/$arch"
            GOOS="$os" GOARCH="$arch" go build -trimpath \
              -ldflags "-s -w -X main.version=${VERSION}" \
              -o arch-update-notes .
            tar -czf "dist/arch-update-notes-${VERSION}-${os}-${arch}.tar.gz" \
              arch-update-notes LICENSE README.md
            rm -f arch-update-notes
          done
          cd dist
          sha256sum *.tar.gz > SHA256SUMS
          ls -la

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            dist/SHA256SUMS
